import { PopupOpenDirective } from './popup-open.directive';

describe('PopupOpenDirective', () => {
  it('should create an instance', () => {
    const directive = new PopupOpenDirective({} as any,{} as any);
    expect(directive).toBeTruthy();
  });
});
